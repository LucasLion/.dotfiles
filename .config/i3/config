#This file has been auto-generated by i4-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!
for_window [class="^.*"] border pixel 5
for_window [class="steam"] floating enable
for_window [class="steamwebhelper"] floating enable
gaps inner 4
gaps outer 1

set $mod Mod4
set $Alt Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
# bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
# bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
bindsym XF86KbdBrightnessUp exec --nostartup-id brightnessctl set +10%
bindsym XF86KbdBrightnessDown exec --nostartup-id brightnessctl set 10%-
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5%
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5%
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl --min-val=2 -q set 7%-
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl -q set 7%+
bindsym XF86Tools exec --no-startup-id pulsemixer

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal

bindsym $mod+Return exec --no-startup-id alacritty

bindsym --release $mod+x exec --no-startup-id import /tmp/latest-screenshot.png


# kill focused window
bindsym $mod+q kill

# rofi-beats
#bindsym $mod+m exec --no-startup-id /usr/bin/rofi-beats

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation
bindsym $mod+o split h
bindsym $mod+Shift+o focus left, split h

# split in vertical orientation
bindsym $mod+v split v
bindsym $mod+Shift+v focus up, split v


# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
 bindsym $mod+s layout stacking
 bindsym $mod+w layout tabbed
 bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

bindsym $win+Shift+v focus up, split v
bindsym $win+Shift+h focus left, split h


set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

bindsym $mod+Control+1 move container to workspace $ws1; workspace $ws1
bindsym $mod+Control+2 move container to workspace $ws2; workspace $ws2
bindsym $mod+Control+3 move container to workspace $ws3; workspace $ws3
bindsym $mod+Control+4 move container to workspace $ws4; workspace $ws4
bindsym $mod+Control+5 move container to workspace $ws5; workspace $ws5
bindsym $mod+Control+6 move container to workspace $ws6; workspace $ws6
bindsym $mod+Control+7 move container to workspace $ws7; workspace $ws7
bindsym $mod+Control+8 move container to workspace $ws8; workspace $ws8
bindsym $mod+Control+9 move container to workspace $ws9; workspace $ws9
bindsym $mod+Control+0 move container to workspace $ws0; workspace $ws0

bindsym $mod+Control+m move workspace to output primary
bindsym $mod+Control+s move workspace to output HDMI-1-1 
bindsym $mod+Control+l move workspace to output right
bindsym $mod+Control+h move workspace to output left
bindsym $mod+Control+j move workspace to output down
bindsym $mod+Control+k move workspace to output up

bindsym $mod+Shift+Control+m move workspace to output primary
bindsym $mod+Shift+Control+s move workspace to output HDMI-1-1 
bindsym $mod+Shift+Control+l move workspace to output right
bindsym $mod+Shift+Control+h move workspace to output left
bindsym $mod+Shift+Control+j move workspace to output down
bindsym $mod+Shift+Control+k move workspace to output up

bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Shift+i move scratchpad
bindsym $mod+i scratchpad show

bindsym $mod+r mode "resize"
mode "resize" {
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}



set $bg-color 	         #11121d
set $inactive-bg-color   #00000000
set $text-color          #d2f4c8
set $inactive-text-color #d2f4c8
set $urgent-bg-color     #E53935

# window colors
#                       border              background         text                 indicator
client.focused          $bg-color           $inactive-bg-color          $text-color          $inactive-bg-color 
client.unfocused        $bg-color  $bg-color $inactive-text-color $bg-color
client.focused_inactive $bg-color  $bg-color $inactive-text-color $bg-color
client.urgent           $urgent-bg-color    $urgent-bg-color   $text-color          $br-color

hide_edge_borders both

bar {
        status_command i3blocks -c /home/noil/.i3blocks.conf
        height 25
        workspace_min_width 45
        colors {
        background $bg-color
            separator #757575
        #                  border             background         text
        focused_workspace  $497b77            $ffffff         	 $text-color
        inactive_workspace $497b77            $bg-color          $inactive-text-color
        urgent_workspace   $urgent-bg-color   $urgent-bg-color   $text-color
	}
       font pango:JetBrains Mono 10
}

bindsym $mod+d exec "rofi -modi window,drun,run -show run -sidebar-mode -terminal kitty -color-window '#000000, #000000, #000000' -color-normal '#000000, #b3e774, #000000, #b3e774, #000000' -color-active '#000000, #b3e774, #000000, #b3e774, #000000' -color-urgent '#000000, #b3e774, #000000, #b3e774, #000000'"
bindsym $mod+m exec "/home/$USER/scripts/radio.sh"

exec --no-startup-id picom --config ~/.config/picom.conf -f
exec --no-startup-id xcompmgr -c -f -n
exec --no-startup-id xset r rate 220 30
exec --no-startup-id xset s off -dpms

#exec --no-startup-id killall $HOME/scripts/back4.sh 
# exec --no-startup-id $HOME/scripts/back4.sh auto /home/noil/Images/wallpapers/takestwo.gif

exec --no-startup-id `xinput set-prop "SynPS/2 Synaptics TouchPad" "libinput Tapping Enabled" 1`

